const enum PlanRoundConfig {
  maxSizeK = 0.5,
  minProcDur = 90,
  maxProcDur = 4200,
  minRoundSize = 70,

  fontSizeK = 5.5,
  timeSizeK = 20,
  timeSizeK2 = 2.5,
  // day config
}

export default PlanRoundConfig;
