const enum Path {
  origin = 'http://localhost:5100/api',
  registration = '/users/registration',
  login = '/users/login',
  plans = '/plans',
  update = '/update',
  weekDistribution = '/weekDistribution/get',
}

export default Path;
