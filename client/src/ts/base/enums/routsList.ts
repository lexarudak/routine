const enum RoutsList {
  homePage = '/',
  planPage = '/plan',
  profilePage = '/profile',
  accountPage = '/account',
  loginPage = '/login',
  confirmPage = '/confirm',
  monday = '/monday',
}

export default RoutsList;
