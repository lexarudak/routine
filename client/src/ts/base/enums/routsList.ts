const enum RoutsList {
  homePage = '/',
  planPage = '/plan',
  profilePage = '/profile',
  accountPage = '/account',
  loginPage = '/login',
  monday = '/monday',
  confirmPage = '/confirm',
}

export default RoutsList;
